#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# запускаем эту библиотеку, чтоб она проверяла только те файлы, которые изменились
npx lint-staged
npm run test:unit

# это предназначено, чтоб делать проверки перед пушем в репозиторий
# здесь должны запускаться проверки линтинга, т.к. остальные проверки слишком долгие для прекоммитов. В данном случае мы можем запускать все для того, чтоб все прогонялось по аналогии с гит-экшенами (например, когда лимит экшенов закончился)
# также, перед тем как сделать коммит, необходимо запустить сторибук локально, чтоб не падали проверки сторибука

# Значения, которые будут запускаться
# npm run build:prod
# npm run lint:ts
# npm run lint:scss
# npm run test:unit
# npm run storybook:build
# npm run test:ui