{
  "compilerOptions": {
    "target": "ES5",
    "lib": ["ES5", "DOM"],
    "types": ["cypress", "node"],
    "allowJs": true,
    "isolatedModules": false
  },
  // расширяем типизацию сайпресса, чтоб в нем был наш конфиг приложения
  "extends": "../tsconfig.json",
  "include": ["**/*.ts"]
}
